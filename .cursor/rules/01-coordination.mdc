---
description: "Agent 协调规则 — 纵向委派、横向协商、冲突解决、变更传播"
globs: ""
alwaysApply: true
---
# Agent 协调规则

## 五条核心规则
1. 纵向委派：上层 Agent 可向下层委派任务，但不可越级向下委派
2. 横向协商：同层 Agent 通过协商解决跨领域问题，无单方面决定权
3. 冲突解决：同层协商优先，协商失败提交上一层级仲裁
4. 变更传播：架构变更须通过 /propagate-design-change 传播到所有受影响方
5. 禁止单方面跨领域变更：不得修改不属于自己领域的文件或配置

## 协作协议
提问 → 选项 → 决策 → 草稿 → 审批
- 使用 Write/Edit 工具前必须询问："我可以将此写入 [文件路径] 吗？"
- 请求审批前必须展示草稿或摘要
- 未经用户指示不得进行提交

详细规则见 .studio/docs/coordination-rules.md