---
description: "编码规范 — 命名约定、禁止模式、文件命名、提交规范"
globs: ""
alwaysApply: true
---
# 编码规范

## 命名约定
- 前端组件：PascalCase（如 UserProfile.tsx）
- 前端样式：kebab-case（如 user-profile.css）
- 后端模块：按语言惯例（Python: snake_case，Java/Go: camelCase/PascalCase）
- 测试文件：[被测名].test.[ext] 或 [被测名].spec.[ext]
- 文档文件：kebab-case

## 禁止模式
- 禁止硬编码环境配置（数据库连接、API 密钥、端口等）
- 禁止使用 any 类型（TypeScript）
- 禁止在生产代码中使用 console.log
- 禁止空 catch 块
- 禁止使用已弃用的 API 或依赖

## 提交规范
遵循 Conventional Commits：feat/fix/docs/refactor/test/chore
Subject 不超过 50 字。禁止向 main/master 直接推送。

详细规范见 .studio/project/code-style.md