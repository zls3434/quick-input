[package]
name = "quickinput-config"
version = "0.1.0"
edition = "2021"
description = "QuickInput 配置管理模块 — TOML 配置数据结构与校验规则"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"  # 用于测试中的序列化验证
toml = "0.8"

[dev-dependencies]