# 源代码目录

本目录包含项目的源代码，按层级组织：
- `frontend/` — 前端代码（React/Vue/Angular 等）
- `backend/` — 后端代码（Node.js/Python/Java/Go 等）
- `shared/` — 前后端共享的类型定义、工具函数等

## 编码规范

请遵循 `.claude/docs/coding-standards.md` 中的规范。
路径范围规则在 `.claude/rules/` 中定义。

## 架构决策

所有架构决策记录在 `docs/architecture/` 中。
实现前请先阅读相关 ADR。