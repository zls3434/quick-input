{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for overlay and settings windows",
  "windows": ["overlay", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "opener:default"
  ]
}
