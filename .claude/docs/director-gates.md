<!-- Software Engineering Studios -->
# 总监门禁定义

本文件定义 Software Engineering Studios 中总监层的 5 个门禁（Gate）。门禁是总监层 Agent 对关键阶段产出物的质量把关机制，必须在进入下一阶段前通过对应门禁。

## 1. 门禁概览

| 门禁 ID | 负责总监 | 用途 | 判定标准 |
| --- | --- | --- | --- |
| PD-PILLARS | 产品总监 | 验证产品支柱 | APPROVE / CONCERNS / REJECT |
| PD-CONCEPT-FIT | 产品总监 | 产品概念对齐 | APPROVE / CONCERNS / REJECT |
| CA-ARCHITECTURE | 首席架构师 | 验证架构 | APPROVE / CONCERNS / REJECT |
| CA-FEASIBILITY | 首席架构师 | 技术可行性评估 | APPROVE / CONCERNS / REJECT |
| PM-SCOPE | 项目经理 | 范围评估 | APPROVE / CONCERNS / REJECT |
| CA-ASSET | 首席架构师 | 资产扩展审核 | APPROVE / CONCERNS / REJECT |

**门禁判定含义：**

- **APPROVE**：通过门禁，可进入下一阶段
- **CONCERNS**：有保留意见，可在修复后继续，但需记录关注项
- **REJECT**：不通过，必须修复后重新提交门禁审查

---

## 2. PD-PILLARS（产品支柱验证）

**负责总监：** `product-director`（产品总监）

### 触发条件

- 产品概念文档生成后
- 产品支柱定义完成时
- 产品方向发生重大调整时

### 传入数据

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| 支柱集 | 列表 | 产品支柱定义列表，每个支柱含名称、描述、成功指标 |
| 设计测试 | 文档 | 针对支柱设计的验证测试方案 |

### 判定标准

| 判定 | 条件 |
| --- | --- |
| **APPROVE** | 支柱定义清晰、可度量、与产品愿景一致，设计测试可验证支柱达成 |
| **CONCERNS** | 支柱定义基本清晰但部分指标不够可度量，或设计测试覆盖不全 |
| **REJECT** | 支柱定义模糊、不可度量、与产品愿景不一致，或缺失设计测试 |

### 输出格式

```markdown
## 门禁审查报告：PD-PILLARS

- 审查时间：[时间戳]
- 审查者：product-director
- 判定结果：[APPROVE / CONCERNS / REJECT]

### 支柱逐项评估

| 支柱名称 | 评估 | 说明 |
| --- | --- | --- |
| [支柱1] | [通过/关注/否决] | [详细说明] |
| [支柱2] | [通过/关注/否决] | [详细说明] |

### 设计测试评估

- 覆盖性：[评估]
- 可度量性：[评估]
- 可执行性：[评估]

### 关注项 / 修复项

1. [关注项描述及建议]
2. [关注项描述及建议]

### 结论

[判定理由与后续行动建议]
```

---

## 3. PD-CONCEPT-FIT（产品概念对齐）

**负责总监：** `product-director`（产品总监）

### 触发条件

- 产品概念文档（`design/requirements/product-concept.md`）生成后
- 产品支柱验证通过后
- 准备进入模块映射前

### 传入数据

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| 概念文档 | 文件引用 | `design/requirements/product-concept.md` 路径 |
| 支柱 | 列表 | 已验证通过的产品支柱集 |

### 判定标准

| 判定 | 条件 |
| --- | --- |
| **APPROVE** | 概念文档与支柱完全对齐，目标用户清晰，核心功能覆盖所有支柱 |
| **CONCERNS** | 概念文档基本对齐但部分功能与支柱关联不明确，或目标用户描述不够具体 |
| **REJECT** | 概念文档与支柱不对齐，核心功能缺失关键支柱支持，或目标用户定义不清 |

### 输出格式

```markdown
## 门禁审查报告：PD-CONCEPT-FIT

- 审查时间：[时间戳]
- 审查者：product-director
- 判定结果：[APPROVE / CONCERNS / REJECT]

### 概念与支柱对齐分析

| 支柱 | 对应概念功能 | 对齐评估 | 说明 |
| --- | --- | --- | --- |
| [支柱1] | [功能名] | [对齐/部分对齐/不对齐] | [说明] |
| [支柱2] | [功能名] | [对齐/部分对齐/不对齐] | [说明] |

### 目标用户评估

- 用户画像清晰度：[评估]
- 用户需求覆盖：[评估]
- 用户价值主张：[评估]

### 核心功能评估

- 功能完整性：[评估]
- 功能优先级合理性：[评估]
- MVP 范围建议：[评估]

### 关注项 / 修复项

1. [关注项描述及建议]
2. [关注项描述及建议]

### 结论

[判定理由与后续行动建议]
```

---

## 4. CA-ARCHITECTURE（架构验证）

**负责总监：** `chief-architect`（首席架构师）

### 触发条件

- 架构文档创建完成后
- 至少 3 条 ADR 已记录
- 准备进行架构审查时

### 传入数据

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| 架构文档 | 文件引用 | `docs/architecture/overview.md` 路径 |
| ADR 列表 | 文件列表 | `docs/architecture/decisions/ADR-*.md` 文件列表 |

### 判定标准

| 判定 | 条件 |
| --- | --- |
| **APPROVE** | 架构设计完整合理，ADR 至少 3 条且论证充分，技术选型与需求匹配，模块划分清晰 |
| **CONCERNS** | 架构基本合理但部分模块边界不清晰，或部分 ADR 论证不够充分，或技术选型存在风险 |
| **REJECT** | 架构设计存在重大缺陷，ADR 不足 3 条或缺失关键决策，技术选型与需求不匹配 |

### 输出格式

```markdown
## 门禁审查报告：CA-ARCHITECTURE

- 审查时间：[时间戳]
- 审查者：chief-architect
- 判定结果：[APPROVE / CONCERNS / REJECT]

### 架构文档评估

- 架构概述完整性：[评估]
- 模块划分合理性：[评估]
- 技术选型适配性：[评估]
- 可扩展性考量：[评估]
- 可维护性考量：[评估]

### ADR 评估

| ADR 编号 | 标题 | 评估 | 说明 |
| --- | --- | --- | --- |
| ADR-001 | [标题] | [充分/部分充分/不充分] | [说明] |
| ADR-002 | [标题] | [充分/部分充分/不充分] | [说明] |
| ADR-003 | [标题] | [充分/部分充分/不充分] | [说明] |

- ADR 数量：[实际数量] / 最低要求：3

### 风险评估

| 风险项 | 严重程度 | 缓解措施建议 |
| --- | --- | --- |
| [风险1] | [高/中/低] | [措施] |

### 关注项 / 修复项

1. [关注项描述及建议]
2. [关注项描述及建议]

### 结论

[判定理由与后续行动建议]
```

---

## 5. CA-FEASIBILITY（技术可行性评估）

**负责总监：** `chief-architect`（首席架构师）

### 触发条件

- 核心流程设计完成时
- 技术选型阶段
- 面对新技术或高风险技术决策时

### 传入数据

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| 核心流程描述 | 文档 | 核心业务流程的详细描述，包括关键路径和数据流 |
| 目标平台 | 列表 | 部署平台和运行环境信息 |
| 技术风险 | 列表 | 已识别的技术风险清单 |

### 判定标准

| 判定 | 条件 |
| --- | --- |
| **APPROVE** | 核心流程技术可行，目标平台支持所有需求，技术风险可控且有缓解方案 |
| **CONCERNS** | 核心流程基本可行但部分环节存在技术不确定性，或部分技术风险缓解方案不够完善 |
| **REJECT** | 核心流程存在不可行的技术环节，或目标平台不支持关键需求，或技术风险不可控 |

### 输出格式

```markdown
## 门禁审查报告：CA-FEASIBILITY

- 审查时间：[时间戳]
- 审查者：chief-architect
- 判定结果：[APPROVE / CONCERNS / REJECT]

### 核心流程可行性分析

| 流程环节 | 技术方案 | 可行性评估 | 说明 |
| --- | --- | --- | --- |
| [环节1] | [方案] | [可行/部分可行/不可行] | [说明] |
| [环节2] | [方案] | [可行/部分可行/不可行] | [说明] |

### 目标平台兼容性

| 平台要求 | 目标平台支持 | 评估 |
| --- | --- | --- |
| [要求1] | [支持/部分支持/不支持] | [说明] |

### 技术风险评估

| 风险项 | 严重程度 | 可能性 | 缓解方案 | 评估 |
| --- | --- | --- | --- | --- |
| [风险1] | [高/中/低] | [高/中/低] | [方案] | [可控/部分可控/不可控] |

### 建议

1. [技术建议]
2. [技术建议]

### 结论

[判定理由与后续行动建议]
```

---

## 6. PM-SCOPE（范围评估）

**负责总监：** `project-manager`（项目经理）

### 触发条件

- 产品概念和架构设计完成后
- MVP 定义阶段
- Sprint 规划前
- 范围变更请求时

### 传入数据

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| 完整愿景 | 文件引用 | 产品愿景文档路径 |
| MVP 定义 | 文档 | 最小可行产品范围定义，包含功能清单和优先级 |
| 时间线 | 文档 | 项目时间线规划，包含里程碑和截止日期 |
| 团队规模 | 文档 | 可用团队资源和成员配置 |

### 判定标准

| 判定 | 条件 |
| --- | --- |
| **APPROVE** | MVP 范围合理且在时间线内可交付，团队规模匹配工作负载，优先级清晰 |
| **CONCERNS** | MVP 范围基本合理但可能偏大或偏小，或时间线偏紧，或团队资源略有不足 |
| **REJECT** | MVP 范围过大无法在时间线内交付，或团队资源严重不足，或优先级不清晰 |

### 输出格式

```markdown
## 门禁审查报告：PM-SCOPE

- 审查时间：[时间戳]
- 审查者：project-manager
- 判定结果：[APPROVE / CONCERNS / REJECT]

### MVP 范围评估

| 功能项 | 优先级 | 工作量估算 | 是否纳入MVP | 评估 |
| --- | --- | --- | --- | --- |
| [功能1] | [P0/P1/P2] | [人天] | [是/否] | [合理/偏大/偏小] |

### 时间线评估

- 总工期：[天数]
- 里程碑数量：[数量]
- 缓冲时间占比：[百分比]
- 评估：[合理/偏紧/偏松]

### 团队资源评估

- 可用成员数：[人数]
- 技能覆盖：[评估]
- 工作负载匹配：[评估]

### 风险评估

| 风险项 | 影响 | 缓解措施 |
| --- | --- | --- |
| [风险1] | [高/中/低] | [措施] |

### 建议

1. [范围调整建议]
2. [资源调配建议]

### 结论

[判定理由与后续行动建议]
```

---

## 7. 门禁执行流程

1. **触发门禁**：对应阶段产出物就绪后，由负责 Agent 调用门禁检查
2. **传入数据**：收集所需传入数据并提交至总监 Agent
3. **总监审查**：总监 Agent 对照判定标准进行审查
4. **输出报告**：生成门禁审查报告并保存至 `production/` 对应目录
5. **判定处理**：
   - APPROVE → 继续下一阶段
   - CONCERNS → 记录关注项，修复后继续
   - REJECT → 修复后重新提交门禁审查
6. **更新状态**：在 `active.md` 中记录门禁结果

### 7.1 资产扩展门禁（CA-ASSET）

**负责总监：** `chief-architect`（首席架构师）

### 触发条件

- 通过 `/create-agent`、`/create-skill` 或 `/create-rule` 创建资产草案后
- 调用 `/asset-review` 命令时

### 传入数据

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| 资产类型 | 字符串 | agent / skill / rule |
| 资产名称 | 字符串 | kebab-case 资产名称 |
| 提案文档 | 文件引用 | `.studio/registry/proposals/` 下的提案文件 |
| 草稿文件 | 文件引用 | 资产定义草稿文件路径 |

### 判定标准

| 判定 | 条件 |
| --- | --- |
| **APPROVE** | 新资产职责清晰、不与现有资产重叠、模式合规、有明确需求驱动 |
| **CONCERNS** | 基本合理但部分规范不完整或职责边界略模糊，可通过修改草稿解决 |
| **REJECT** | 职责与现有资产重叠、模式不合规、缺乏明确需求驱动 |

### 审核检查清单

**Agent**：职责不重叠 / 模型层级合理 / 委托图完整 / 协作协议明确 / Body 六章节完整 / platforms 配置正确

**Skill**：名称合规（kebab-case、1-64 字符、不重复）/ frontmatter 完整 / Body 五章节完整 / 模型层级合理 / 不与现有技能重复

**Rule**：路径不冲突 / 规范条目清晰 / 示例完整（正确+错误）/ platforms 配置正确

### 输出

审核报告保存至 `.studio/registry/reviews/[资产类型]-[资产名称]-review.md`，并更新 `asset-registry.yaml` 中条目状态为 `reviewed`。

完整规范详见 `docs/extension-mechanism.md`。

## 8. 门禁与阶段映射

| 阶段 | 对应门禁 |
| --- | --- |
| 需求阶段 | PD-PILLARS、PD-CONCEPT-FIT |
| 架构设计阶段 | CA-ARCHITECTURE、CA-FEASIBILITY |
| 预开发阶段 | PM-SCOPE |
| 发布阶段 | gate-check（由 `/gate-check` 命令触发，综合所有门禁结果） |