# API 规范

> 本模板用于定义 API 的端点、认证方式、版本管理与使用示例。

---

## 元数据

| 字段 | 内容 |
|------|------|
| 文档版本 | [此处填写] |
| 作者 | [此处填写] |
| 更新日期 | [此处填写 YYYY-MM-DD] |
| API 版本 | [此处填写，如 v1] |
| 基础路径 | [此处填写，如 https://api.example.com/v1] |
| 状态 | [草案/评审中/已确认] |

---

## 1. 通用约定

### 1.1 请求格式

- **Content-Type**：`application/json; charset=utf-8`
- **字符集**：UTF-8

### 1.2 响应格式

```json
{
  "code": "[此处填写业务码]",
  "message": "[此处填写提示信息]",
  "data": { },
  "requestId": "[此处填写请求追踪 ID]"
}
```

### 1.3 错误码总表

| 错误码 | 含义 | HTTP 状态码 |
|--------|------|-------------|
| 0 | 成功 | 200 |
| 40001 | 参数校验失败 | 400 |
| 40101 | 未认证 | 401 |
| 40301 | 无权限 | 403 |
| 40401 | 资源不存在 | 404 |
| 50001 | 服务器内部错误 | 500 |

---

## 2. 认证方式

- **认证类型**：[此处填写，如 Bearer Token / API Key / OAuth2]
- **获取方式**：[此处填写]
- **请求头示例**：

```
Authorization: Bearer [此处填写 token]
```

- **Token 有效期**：[此处填写]
- **刷新机制**：[此处填写]

---

## 3. 版本管理

- **版本策略**：[此处填写，如 URL 路径版本 / Header 版本]
- **当前版本**：[此处填写]
- **废弃策略**：[此处填写]
- **兼容性规则**：[此处填写]

---

## 4. 端点定义

### 4.1 端点 EP-01：[此处填写端点名]

- **方法**：[GET/POST/PUT/PATCH/DELETE]
- **路径**：[此处填写，如 /resources/{id}]
- **描述**：[此处填写]
- **认证**：[是/否]

#### 请求参数

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| [此处填写] | [此处填写] | [是] | [此处填写] |

**查询参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| [此处填写] | [此处填写] | [是/否] | [此处填写] | [此处填写] |

**请求体**：

```json
{
  "[此处填写字段]": "[此处填写类型]"
}
```

#### 响应

**成功响应（200）**：

```json
{
  "code": 0,
  "data": {
    "[此处填写字段]": "[此处填写类型]"
  }
}
```

**错误响应**：

| HTTP 状态 | 错误码 | 说明 |
|-----------|--------|------|
| 400 | 40001 | [此处填写] |
| 404 | 40401 | [此处填写] |

---

### 4.2 端点 EP-02：[此处填写端点名]

- **方法**：[此处填写]
- **路径**：[此处填写]
- **描述**：[此处填写]
- **认证**：[是/否]

#### 请求参数

[此处填写，参照 EP-01 格式]

#### 响应

[此处填写]

---

## 5. 使用示例

### 5.1 示例：[此处填写场景]

**请求**：

```bash
curl -X GET https://api.example.com/v1/resources/1 \
  -H "Authorization: Bearer [token]"
```

**响应**：

```json
{
  "code": 0,
  "data": { }
}
```

---

## 6. 限流与配额

| 维度 | 限制 | 说明 |
|------|------|------|
| 全局 | [此处填写，如 1000 QPS] | [此处填写] |
| 单租户 | [此处填写] | [此处填写] |
| 单用户 | [此处填写] | [此处填写] |

---

## 附录

- **变更历史**：

| 日期 | 修改人 | 修改内容 |
|------|--------|----------|
| [此处填写] | [此处填写] | [此处填写] |